# Python
__pycache__/
*.py[cod]
*.so
.Python

# Virtual environments
env/
venv/

# Jupyter
.ipynb_checkpoints/

# Data
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# Decision-layer verdict store — live runtime trail, not source (SQLite + journal/WAL)
data/*.db
data/*.db-*

# Models
models/

# Dotenv
.env

# MS Office temp/lock files
~$*
~*.tmp
*.tmp

# Control Advisor generated reports (test output, not source)
control-advisor/reports/
control-advisor/scan_report.json

# Docker log snapshots (runtime artifacts, not source) — but keep the tooling
# scripts and the one committed evidence snapshot that backs Exhibit 14.
docs/logs/*
!docs/logs/.gitkeep
!docs/logs/*.ps1
!docs/logs/*.py
!docs/logs/2026-07-05_120350/
!docs/logs/2026-07-05_120350/**

# Claude Code local session state
.claude/

# Scientific paper draft — kept local, not published to GitHub
paper/
dist/
